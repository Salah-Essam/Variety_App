import 'package:flutter/material.dart';

class FigmaFavorites extends StatelessWidget {
  const FigmaFavorites({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(backgroundColor: Colors.red);
  }
}
