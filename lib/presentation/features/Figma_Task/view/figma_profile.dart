import 'package:flutter/material.dart';

class FigmaProfile extends StatelessWidget {
  const FigmaProfile({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(backgroundColor: Colors.blue);
  }
}
